{
  "name": "crud-factory",
  "description": "Cloudflare monorepo codebase for customer CRUD apps (multi-stack, D1, Workers)",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=demo",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "new-app": "node scripts/new-app.mjs",
    "stacks": "node -e \"console.log(JSON.stringify(require('./platform/stacks.json'),null,2))\"",
    "db:push": "pnpm --filter demo db:push"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "turbo": "^2.10.12",
    "typescript": "7.0.2"
  },
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=20"
  }
}
